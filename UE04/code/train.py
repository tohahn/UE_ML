import football as f

if __name__ == "__main__":
	ole = f.Expression("Ole")
	tor = f.Expression("Toor!")
	oh = f.Expression("Ohhh")
	
	f.recognize(ole)
	f.recognize(ole)
	f.recognize(oh)
	f.recognize(ole)
	f.recognize(tor)
